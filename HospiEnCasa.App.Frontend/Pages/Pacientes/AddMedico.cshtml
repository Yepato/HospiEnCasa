@page
@model HospiEnCasa.App.Frontend.Pages.Pacientes.AddMedicoModel
@{
}
<h1>Asignar Médico a Paciente</h1>
<form method="post">
    <div class="form-group">
        <label for = "idPaciente">Paciente</label>
        <input type = "text" id = "idPaciente" name = "idPaciente" class = "form-control" value = "@Model.paciente.Id" readonly>
    </div>
    <div class="form-group">
        <label for = "idMedico">Médico</label>
        <select id = "idMedico" name = "idMedico" class = "form-control">
            @foreach (var medico in Model.medicos)
            {
                <option value = "@medico.Id">@medico.Nombre</option>
            }
        </select>    
    </div>
    <br>
    <button type = "submit" class = "btn btn-primary">Asignar Médico</button>
    <br>
    <br>
</form>
<a class = "btn btn-secondary" asp-page = "./Index">Volver</a>
